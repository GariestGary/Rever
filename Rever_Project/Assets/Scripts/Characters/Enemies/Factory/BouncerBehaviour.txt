tree("Root")
	Fallback
		tree("Chase")
		tree("Bounce")

tree("Bounce")
	Sequence
		GroundedCheck
		FallBack
			Sequence
				RightDirection
				Fallback
					Sequence
						CheckDestination(1)
						JumpRight
					ChangeDirection
			Sequence
				Fallback
					Sequence
						CheckDestination(-1)
						JumpLeft
					ChangeDirection
		Random
			Wait(1.5)
			Wait(3.0)
			Wait(1.6)
			Wait(2.0)
			Wait(1.8)

tree("Chase")
	Sequence
		IsPlayerNoticed
		Fallback
			Sequence
				GroundedCheck
				JumpToPlayer
			Random
				Wait(0.8)
				Wait(0.9)
				Wait(1.0)
				Wait(1.1)
				Wait(1.2)
				Wait(1.3)
				Wait(1.4)




